### Longest Prefix Suffix

**Problem Statement:**

Given a string `s`, find the length of the longest prefix which is also a suffix. Note that the prefix and suffix can be overlapping, but they should not be equal to the entire string.

### Input:

- A string `s` consisting of lowercase English alphabets (1 ≤ `s.size()` ≤ 10^6).

### Output:

- Return the length of the longest prefix which is also a suffix. The prefix and suffix must not be equal to the entire string.

### Examples:

**Example 1:**

- Input: `s = "abab"`
- Output: `2`
- Explanation: The longest prefix and suffix is `"ab"`, which has a length of 2.

**Example 2:**

- Input: `s = "aabcdaabc"`
- Output: `4`
- Explanation: The longest prefix and suffix is `"aabc"`, which has a length of 4.

**Example 3:**

- Input: `s = "aaaa"`
- Output: `3`
- Explanation: The longest prefix and suffix is `"aaa"`, which has a length of 3.

### Constraints:

- `1 ≤ s.size() ≤ 10^6`
